var nodes = zTreeObj.getNodes();
	console.log(nodes)
	if (nodes.length > 0) {
		for (n of nodes) {
			if (n.children.length > 0) {
				for (c of n.children) {
					if (c.children.length > 0) {
						for (i of c.children) {
							i.checked = false
							zTreeObj.updateNode(i);
						}
					}

				}
				
			}
		}

	}