개념 설계(정규화)  ->데이터베이스, erd
내부 물리적으로 구성(인덱스예시)


개념적 설계->논리적 설계 (정규화)->물리적 설계(실제로테이블도만들고 인덱스도만들고)



오브젝트를 통해서 클래스를 구현하고 그후에 인스턴스를생성한다
따라서,
오브젝트- 객체
인스턴스- 객체란 말을 쓰면안됨


-- 아래는 같은 뜻
 record set(자바 collection set처럼 순서가없음)/행/튜플


카디널리티는 행의 수



후보키
- 유일성(unique)
- 최소성 : 최소로 적은 갯수로 구성해야 한다.(예시 복합키)


후보키는 null가능
후보키+not null=기본키



관계대수

select   ->where절
project -> column절
join ->  크로스조인(카티션 프로덕트)



중복된걸 제거하면 division
안하면 join





함수적 종속이란 
-> 기본키에 따라 속성이 바뀌어야만 하는 것.
기본키(X)  컬럼,속성(Y)


부분함수적 종속-> 1정규화  partial (데이터중복값 X,기본키 O)

부분 종속을 제거한 것이 2정규화 (완전 함수적 종속) full

  transitive
이행적 함수란? 컬럼이나 속성이 일반컬럼에 종속된 것
이행적 함수적 종속을 제거하면 3정규화  





